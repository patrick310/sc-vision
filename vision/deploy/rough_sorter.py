#Import packages
from keras.models import load_model

#Load the model and weight

#Start a picture taking loop

#Take a picture from the ring buffer, and evaluate it against the model

#If image is *good (read: clear, non-blurry, subject visibile, confidence high) move to folder (discard if image is bad)


class RoughSorter():

	def __init__():
		None
		
	def import_model():
		self.model = load_model('model_output.h5')
		
	def begin_photo_loop():
		None
	
	def end_photo_loop():
		None
	
	def pop_new_image_from_buffer():
		None
		
	def image_is_clear():
		None
		
	def evaluate_image_to_model():
		None
		
	def save_image_to_file():
		None